#include <stdio.h>

int encript(int e);

int decript(int d);

int main() 
  {
      
  return O;
  }

int encript(int x) {
    int m, key, e;
    e=(m+k)(%k);
    return e;
}

int decript(int y) {
    int c, key, d;
    d=(c-k)*(%*26);
    return d;
}